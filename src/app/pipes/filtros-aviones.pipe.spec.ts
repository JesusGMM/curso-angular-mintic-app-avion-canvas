import { FiltrosAvionesPipe } from './filtros-aviones.pipe';

describe('FiltrosAvionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosAvionesPipe();
    expect(pipe).toBeTruthy();
  });
});
