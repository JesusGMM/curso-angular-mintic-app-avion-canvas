import { FiltrosUsuariosPipe } from './filtros-usuarios.pipe';

describe('FiltrosUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
